@using Functionland.FxBlox.Shared.Infra
<CascadingAuthenticationState>
    <LayoutView Layout="@typeof(MainLayout)">
        <Router AppAssembly="@GetType().Assembly"
                AdditionalAssemblies="@(new[] { Assembly.Load(BlazorModeDetector.Current.IsBlazorHybrid() ? "Functionland.FxBlox.Client.App" : "Functionland.FxBlox.Client.Web") })"
                OnNavigateAsync="@OnNavigateAsync">
            <Found Context="routeData">
                <AuthorizeRouteView RouteData="@routeData">
                    <NotAuthorized>
                        <NotAuthorizedComponent />
                    </NotAuthorized>
                    <Authorizing>
                    </Authorizing>
                </AuthorizeRouteView>
            </Found>
            <NotFound>
                <PageNotFound />
            </NotFound>
            <Navigating>
            </Navigating>
        </Router>
    </LayoutView>
</CascadingAuthenticationState>